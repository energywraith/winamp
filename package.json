{
  "name": "winamp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "client-install": "cd client && npm install",
    "server-install": "cd server && npm install",
    "install": "concurrently \"npm run server-install\" \"npm run client-install\"",
    "dev": "concurrently \"cd client && npm run serve\" \"cd server && npm run dev\"",
    "start": "rm -rf server/views/* && cd client && npm run build && cp -r dist/* ../server/views && cd ../server && npm start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.3.0"
  }
}
