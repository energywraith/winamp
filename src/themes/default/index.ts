import type { Theme } from "..";

const colors = {
  appBackground: "#000000",
};

const size = {
  width: "273px",
  height: "150px",
};

const theme: Theme = {
  colors,
  size,
};

export default theme;
